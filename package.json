{
  "name": "telegram-music-bot",
  "private": true,
  "description": "Monorepo: deploy Mini App on Vercel with Root Directory = frontend (vercel.json outputDirectory is dist). API and bot deploy separately.",
  "scripts": {
    "build": "npm ci --prefix frontend && npm run build --prefix frontend"
  },
  "engines": {
    "node": ">=18"
  }
}
