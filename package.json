{
  "name": "telegram-music-bot",
  "private": true,
  "description": "Monorepo: on Vercel leave Root Directory empty (repo root); build outputs to frontend/dist. API and bot deploy separately.",
  "scripts": {
    "build": "npm ci --prefix frontend && npm run build --prefix frontend"
  },
  "engines": {
    "node": ">=18"
  }
}
